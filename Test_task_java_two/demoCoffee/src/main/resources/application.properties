coffee.complete.message=Coffee is Ready.
coffee.notComplete.message=Not Enough Ingredients. Check the water level and the amount of coffee.
coffee.waterTank.message=Water Tank Full.
coffee.coffeeTank.message=Coffee Tank Full.
coffee.fillError.message=Error 0x001 has occurred. Try filling the tanks at the same time.
# ===============================
# DATABASE
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=user
spring.datasource.password=pgdb
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Fix Postgres JPA Error:
# Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented.
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
